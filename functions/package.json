{
  "name": "circulares-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo 'Sin lint configurado'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "firebase-admin": "^12.1.1",
    "firebase-functions": "^5.0.1",
    "pdf-parse": "^1.1.1"
  }
}
